[tool.poetry]
name = "amlynx"
version = "0.1.0"
description = "AMLynx — Adaptive Deep-Learning Anti-Money Laundering Engine (dev skeleton)"
authors = ["Your Name <you@example.com>"]
packages = [{include = "src"}]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.95.0"
uvicorn = {extras = ["standard"], version = "^0.22.0"}
sqlalchemy = "^1.4"
alembic = "^1.11"
psycopg2-binary = "^2.9"
pydantic = "^2.2"
pandas = "^2.1"
numpy = "^1.26"
pytest = "^7.4"
python-dotenv = "^1.0"
loguru = "^0.7.0"

[tool.poetry.dev-dependencies]
black = "^24.3"
isort = "^6.0"
ruff = "^0.12.0"
mypy = "^1.5"

[build-system]
requires = ["poetry-core>=1.8.0"]
build-backend = "poetry.core.masonry.api"
